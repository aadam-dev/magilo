# Git hooks

- **commit-msg** – Cleans commit messages (e.g. removes certain Co-authored-by lines).

This repo uses `core.hooksPath = .githooks`, so these hooks run automatically. No install step needed after clone.
